const obj = {
    a:1,
    b:2,
    c:3
}
const result = Object.keys(obj) //[ 'a', 'b', 'c' ]
const result = Object.values(obj) // [ 1, 2, 3 ] 
const result = Object.entries(obj) //[ [ 'a', 1 ], [ 'b', 2 ], [ 'c', 3 ] ]
console.log(result, "result");

